#include <bits/stdc++.h>
using namespace std;

int main(){

    int x, y, z;
    cin >> x >> y >> z;

    int i=0, j=0, k=0;
    for ( ; i<x ; i++){
        for ( ; j<y ; j++){
            for ( ; k<z ; k++){
                // code
            }
        }
    }

    return 0;
}